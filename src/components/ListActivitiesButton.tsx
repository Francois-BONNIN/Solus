import { Button } from "@mantine/core";

function ListActivitiesButton() {
    return (
        <Button color="dark" radius="lg" size="xl">
          Settings
        </Button>
      );
}

export default ListActivitiesButton;